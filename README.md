# Deep_AI_Agent
Building a Deep Agent System using LangChain &amp; Groq -  recently built a Deep Agent architecture that can take a high-level goal, break it down into structured steps, execute them through sub-agents, and store the results in memory for future reasoning.
